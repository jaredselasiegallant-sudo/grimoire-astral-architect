<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Grimoire.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Grimoire: Astral Architect"
    Width="1400" Height="800"
    Background="#050510">

    <Grid KeyDown="MainWindow_KeyDown">
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition Height="*" />
            <RowDefinition Height="140" />
        </Grid.RowDefinitions>

        <!-- TOP BAR -->
        <Border Grid.Row="0" Background="{StaticResource PanelBackgroundBrush}"
                BorderBrush="{StaticResource PanelBorderBrush}" BorderThickness="0,0,0,1"
                Padding="16,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="GRIMOIRE: ASTRAL ARCHITECT"
                           Foreground="{StaticResource GlowCyanBrush}"
                           FontSize="16" FontWeight="Bold" VerticalAlignment="Center" />

                <TextBlock x:Name="SpellStatusText" Grid.Column="1"
                           Text="Draw a shape to cast a spell"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           FontSize="13" VerticalAlignment="Center" HorizontalAlignment="Center" />

                <TextBlock x:Name="CooldownText" Grid.Column="2"
                           Text=""
                           Foreground="{StaticResource AccentBrush}"
                           FontSize="11" VerticalAlignment="Center" Margin="12,0" />

                <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,4,0">
                    <TextBlock Text="&#x2726;" Foreground="{StaticResource GlowPurpleBrush}" FontSize="18" VerticalAlignment="Center" Margin="0,0,6,0" />
                    <TextBlock x:Name="ManaText" Text="0"
                               Foreground="{StaticResource TextPrimaryBrush}" FontSize="15" FontWeight="SemiBold" VerticalAlignment="Center" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- MAIN CONTENT -->
        <Grid Grid.Row="1" x:Name="MainContentGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="320" />
            </Grid.ColumnDefinitions>

            <!-- Canvas host - SKElement added in code-behind -->
            <Border Grid.Column="0" x:Name="CanvasHost" Background="#050510" Margin="4" />

            <!-- Right Panel -->
            <Grid Grid.Column="1" Margin="0,4,4,4">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="8" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="8" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="{StaticResource PanelBackgroundBrush}"
                        BorderBrush="{StaticResource PanelBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="12">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="INVENTORY"
                                   Foreground="{StaticResource GlowCyanBrush}" FontSize="12" FontWeight="Bold"
                                   Margin="0,0,0,8" />
                        <TextBlock Grid.Row="1" Text="No items yet"
                                   Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" />
                    </Grid>
                </Border>

                <Border Grid.Row="1" Background="Transparent" />

                <Border Grid.Row="2" Background="{StaticResource PanelBackgroundBrush}"
                        BorderBrush="{StaticResource PanelBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="12">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="ALCHEMICAL CAULDRON"
                                   Foreground="{StaticResource GlowPurpleBrush}" FontSize="12" FontWeight="Bold"
                                   Margin="0,0,0,12" />
                        <TextBlock x:Name="CraftingResultText" Grid.Row="1"
                                   Text="Place two items in the cauldron to craft"
                                   Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" TextWrapping="Wrap" Margin="0,0,0,8" />
                        <Button x:Name="CraftButton" Grid.Row="2" Content="&#x26A1; CRAFT" HorizontalAlignment="Stretch"
                                Background="{StaticResource GlowPurpleBrush}" Foreground="White" FontWeight="Bold"
                                Margin="0,4" Padding="8,6"
                                Click="CraftButton_Click"
                                IsEnabled="False" />
                    </Grid>
                </Border>

                <Border Grid.Row="3" Background="Transparent" />

                <Border Grid.Row="4" Background="{StaticResource PanelBackgroundBrush}"
                        BorderBrush="{StaticResource PanelBorderBrush}" BorderThickness="1" CornerRadius="8"
                        Padding="12" MaxHeight="120">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="EXPEDITION LOG"
                                   Foreground="{StaticResource GlowCyanBrush}" FontSize="12" FontWeight="Bold"
                                   Margin="0,0,0,6" />
                        <TextBlock x:Name="ExpeditionLogText" Grid.Row="1"
                                   Text="No expeditions yet"
                                   Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"
                                   TextWrapping="Wrap" Opacity="0.8" />
                    </Grid>
                </Border>
            </Grid>
        </Grid>

        <!-- BOTTOM BAR: FAMILIARS -->
        <Border Grid.Row="2" Background="{StaticResource PanelBackgroundBrush}"
                BorderBrush="{StaticResource PanelBorderBrush}" BorderThickness="0,1,0,0" Padding="16,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="FAMILIARS"
                           Foreground="{StaticResource GlowCyanBrush}" FontSize="12" FontWeight="Bold"
                           VerticalAlignment="Center" Margin="0,0,16,0" />

                <TextBlock Grid.Column="1" Text="No familiars hatched yet"
                           Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"
                           VerticalAlignment="Center" />
            </Grid>
        </Border>

        <!-- NOTIFICATION TOAST OVERLAY -->
        <StackPanel x:Name="ToastContainer" Grid.Row="0" Grid.RowSpan="3"
                    VerticalAlignment="Top" HorizontalAlignment="Right"
                    Margin="0,56,24,0" IsHitTestVisible="False" />

        <!-- NARRATIVE OVERLAY -->
        <Border x:Name="NarrativeOverlay" Grid.Row="0" Grid.RowSpan="3"
                Background="#CC0A0A1A" Visibility="Collapsed"
                HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                Padding="60">
            <Grid VerticalAlignment="Center" HorizontalAlignment="Center" MaxWidth="800">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock x:Name="NarrativeSpeaker" Text=""
                               Foreground="{StaticResource GlowPurpleBrush}"
                               FontSize="12" FontWeight="Bold"
                               HorizontalAlignment="Center" Margin="0,0,0,8" />
                    <TextBlock x:Name="NarrativeText" Text=""
                               Foreground="{StaticResource TextPrimaryBrush}"
                               FontSize="20" TextWrapping="Wrap"
                               HorizontalAlignment="Center" TextAlignment="Center"
                               LineHeight="30" Margin="0,0,0,16" />
                    <Button x:Name="NarrativeDismissButton" Content="Continue"
                            HorizontalAlignment="Center" Margin="0,8,0,0"
                            Background="Transparent" Foreground="{StaticResource TextSecondaryBrush}"
                            BorderBrush="{StaticResource PanelBorderBrush}" BorderThickness="1"
                            Padding="24,8"
                            Click="NarrativeDismissButton_Click" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
